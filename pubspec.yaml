name: booktech_flutter
description: "A new Flutter project."
publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: ^3.8.1

dependencies:
  flutter:
    sdk: flutter
  google_fonts: ^6.1.0
  responsive_framework: ^1.1.0 
  provider: ^6.1.1
  flutter_svg: ^2.0.7
  intl: ^0.19.0
  

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0

flutter:
  uses-material-design: true
  assets:
    - assets/lightlogo.png
    - assets/dashboard/
  

    


  
    
